#include "complex.hpp"

using sjtu::complex;
using sjtu::divided_by_zero;

int main() {
  // The OJ may not require any IO; just compile into `code`.
  // We keep main minimal as the problem focuses on the library.
  return 0;
}

